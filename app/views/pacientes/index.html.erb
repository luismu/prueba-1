
 


<div class="page-header">
  <h2 align="center">Lista de Pacientes</h2>
</div>
<div class="table-responsive">
    <%= link_to 'New Paciente', new_paciente_path %>
<table class="table table-striped table-condensed">
  <thead>
    <tr>
    <th>Nombre</th>
      <th>Edad</th>
      <th>Email</th>
      <th>Dirección</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <% @pacientes.each do |paciente| %>
      <tr>
          <td><%= paciente.nombre %></td>
          <td ><%= paciente.edad%></td>
          <td><%= paciente.email%></td>
          <td><%= paciente.direccion %></td><br/>
          
         <td>
             <%= link_to 'Show', paciente , class: "btn btn-default btn-sm"%> 
             <%= link_to 'Edit', edit_paciente_path(paciente), class: "btn btn-success btn-sm" %>
             <%= link_to 'Destroy', paciente, method: :delete, data: { confirm: 'Are you sure?' },class: "btn btn-danger btn-sm" %>
        
          </td>
          <td>
              <%= link_to 'Agregar escala',paciente, class: "btn btn-primary btn-sm" %>
          </td>
      </tr>
     
    <% end %>
  </tbody>
</table>
</div>
<br>


